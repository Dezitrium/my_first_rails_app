<h1>Editing event</h1>

<%= render 'form' %>

<p class="pull-right">
	<%= link_to 'Delete Event', user_event_path(@user, @event), 
								method: 'delete', 
								confirm: 'Are you sure?',
								class: 'btn btn-danger' %>
</p>
