<% if @feed_items.any? %>	
	<ol class="model-lists">
		<%= render partial: 'microposts/feed_item', collection: @feed_items %>
	</ol>
	<%= will_paginate @feed_items, 
			params: { controller: :static_pages, action: :home },			
			inner_window: 2 %>	
<% end %>